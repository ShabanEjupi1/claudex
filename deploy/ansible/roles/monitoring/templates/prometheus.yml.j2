global:
  scrape_interval: 15s

scrape_configs:
  - job_name: node
    static_configs:
      - targets:
{% for host in groups['proliant'] %}
          - "{{ hostvars[host].ansible_host }}:9100"
{% endfor %}

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
